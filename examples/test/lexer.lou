meta testType = @retFun(testType)

final putCStringUnused = @externFun("puts", testType)
final putCString = @externFun("puts", @procFun(@ptr(u8)))

const main = @global("main", fun () -> i32 {
  return fun () -> i32 {
    putCString(c"hello, world!")
    return fun () -> i32 {
      putCString(c"how")
      return fun () -> i32 {
        putCString(c"tf")
        return fun () -> i32 {
          putCString(c"are")
          return fun () -> i32 {
            putCString(c"you?")
            return fun () -> i32 {
              putCString(c"...")
              return 0
            }()
          }()
        }()
      }()
    }()
  }()
})

