public extern const putChar: fun (u8)

public global const main = fun () -> i32 {
  final a: u8 = 97
  final new_line: u8 = 97
  putChar(a)
  putChar(new_line)
  return 0
}

#`
  FIRST_STAGE {
    decl putChar: fun (u8)
    decl main: fun () -> i32
  }

  SECOND_STAGE {
    decl putChar: fun (u8) | extern "putChar"
    decl main: fun () -> i32 | global fun () -> i32 {
      ...
    }
  }
`#
