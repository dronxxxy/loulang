@global("main", fun () -> i32 {
  var a = 12

  # Assignments are also expressions and returns assigned value like in C

  a = a = a = a +    # a = 12
    2 / 2 -          # + 1
    1 * 2 * 2 / 4 -  # - 1
    32 % 20          # + 12

  return a           # = 0
})

