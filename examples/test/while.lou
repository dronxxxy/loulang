const putChar = fun (char: u8) {
  @externFun("putchar", @procFun(u8))(char)
}

@global("main", fun () -> i32 {
  var char = 'a'
  
  while char <= 'z' {
    putChar(char)
    char = char + 1
  }
  putChar('\n')

  return 0
})


