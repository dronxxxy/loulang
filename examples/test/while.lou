const putChar = fun (var int: u8) {
  @externFun("putchar", @procFun(u8))(int)
}

@global("main", fun () -> i32 {
  var char = 'a'
  
  while char <= 'z' {
    putChar(char)
    char = char + 1
  }
  putChar('\n')

  return 0
})


