const main = @global("main", fun () -> i32 {
  var a = 12
  a = a +            # 12
    2 / 2 -          # 1
    1 * 2 * 2 / 4 -  # 1
    32 % 20          # 12
  return a           # should be zero
})

